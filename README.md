# example-sqlalchemy-multiple-engine

This is sqlalchemy sample using multiple engines.

## Setup

Create virtualenv.

```$xslt
$ virtualenv virtualenv
$ source virtualenv/bin/activate
```

Install dependencies.

```$xslt
$ pip install -r requirements.txt
```

## Run

```$xslt
$ python main.py
```

